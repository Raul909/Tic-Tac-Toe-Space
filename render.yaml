services:
  - type: web
    name: tictactoe-multiplayer
    env: node
    plan: free
    rootDir: tictactoe
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

